# Dépendances installées
node_modules

# Fichiers générés par la construction
dist
dist-ssr

# Secrets / clés d'accès (ne JAMAIS envoyer sur GitHub)
.env
.env.local
.env.*.local

# Fabrication de l'APK Android (dépôt PUBLIC : la clé de signature et le
# projet généré par Bubblewrap restent hors de GitHub)
android/

# Fichiers système / éditeurs
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json
