# Dépendances installées
node_modules

# Fichiers générés par la construction
dist
dist-ssr

# Secrets / clés d'accès (ne JAMAIS envoyer sur GitHub)
.env
.env.local
.env.*.local

# APK Android : le projet (android/) est versionné, mais JAMAIS la clé de
# signature (dépôt public !) ni les fichiers produits par la compilation
android/android.keystore
android/.gradle/
android/build/
android/app/build/

# Fichiers système / éditeurs
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json
